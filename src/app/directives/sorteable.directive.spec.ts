import { SorteableDirective } from './sorteable.directive';

describe('SorteableDirective', () => {
  it('should create an instance', () => {
    const directive = new SorteableDirective();
    expect(directive).toBeTruthy();
  });
});
